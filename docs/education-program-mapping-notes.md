# Education program identity and implementation mapping

## Cross-space literacy topics published — 2026-09-11

All 17 audience/grade/administrator/topic query cases now pass in `program-audience-query-verification.json`. New cases test each topic, all four topics combined with OESE + Children + First grade, and exclusion of literacy. Browser review rendered all four topic links on Reading First. These are program-association filters; they do not establish causal component effects or cross-program efficiency ranking.

Reading First now links through root Topics `806d52bc27e94c9193c057978b093351` to four existing Podcast-space concepts: U.S. literacy education `5a86d2f3657b4b5baabef07a3393e408`, Literacy coaches `06d0bc4860de4838a222c240a46fc8b0`, Phonics instruction `09d90bfac91545de8b218e03c026338f`, and Reading comprehension `5c028b0a8a484ee9ae29e07028a1e7d1`. Reviewed names, types, source spaces and contextual backlinks supported reuse; no new topic entities were created. These are program-topic associations, not a complete component inventory, causal mechanisms or an outcome ontology.

Proposal `870bd1a8e2c1400eb8b9ece7faf83c2d` executed and is bounty-linked; all six API checks pass. Main tx `0x645e6b70cd5199c2d2b4af0161341bd07a46fd884c39c04efb9ada0c3e6a23e8`; bounty tx `0xb24fa387ee166db8cd80b26d2ee9cd4be32ed04c3118a1351a10f3e6f16ffd80`; vote `0x6f8727cc112086140786b91eaeee56fc9185a986ac834c1802c75d62c7553ac0`. Model, builder, journal and index evidence use the `reading-first-topics` prefix.

Schema finding: Grant program's Focus areas property is Text and cannot carry topic relations. Root Topics is Relation with Topic range, although Crypto contains conflicting legacy Text metadata on the same property identity. The live property resolver reports Relation; root-scoped ontology is authoritative for this mapping. `literacy-taxonomy-inspection.json` preserves the provenance and bounded backlinks. Always inspect datatype and source space instead of choosing by label.

## Current imported-record reconciliation — 2026-09-11

### Evaluation-method discovery

Final alias search (`regression-discontinuity-alias-discovery.json`) completed all-space/all-type substring queries for discontinuity, RDD and regression-discontinuity. Two discontinuity matches concern Microsoft Surface; RDD matches are city names, gene names and Reddit-stock articles, not the evaluation method. None is a semantic identity match. The RCT concept remains reused. The proposed Regression discontinuity design entity is a root Topic, matching the concept role already used for RCT; no new health-specific research-method type is introduced.

`reading-first-method-model.json` resolves relation semantics by reusing Relation-valued Study design `b1fdc75afb6841a18d8286427099ea44` for design concepts while explicitly scoping each relation entity with Description and Sources. Earlier inconsistent uses are not copied. Reading First retains its existing mixed-design Text value; the new relation means the design is a component of the study. RCT applies at one of 18 sites, regression discontinuity at 17. A frontend design filter must load these scope descriptions and not label the whole evaluation randomized. This is a reviewed model, not yet evidence of publication.

The live search in `study-method-discovery.json` completed all five name searches, including 850 Methods-name candidates. It returned no Regression discontinuity candidate under that substring. The run was slow and initially silent but ultimately exited successfully; it was not interrupted, and the evidence does not establish a Bun/network failure. A separate Node exact-name probe returned promptly. The discovery script now logs each completed term and checkpoints partial results with `complete:false`; partial checkpoints must never establish graph-wide absence.

Do not use Health's Methods relation `47149ca02af74c8cb8fdd66a92fb753b` for evaluation design: its definition concerns intervention delivery techniques, not research methods. The existing Study design relation `b1fdc75afb6841a18d8286427099ea44` is Relation-valued but has no declared range or description. Its complete current usage response contains two edges, both from one News story: one targets a Study category and the other a specific Clinical Trial. This is inconsistent modeling evidence, not a settled reusable design taxonomy. Several other same-named Study design properties are Text, including the one already used for Reading First's mixed-design narrative.

RCT concept `ad9f48f68b804ddd9f9e8ffa5869ef02` is already reused across Health, another research space and Education datasets; its live Topic type and random-assignment definition support identity reuse. Regression discontinuity requires further alias checks before creating a Topic. The source method definition is local to an eligibility cutoff. Reading First's final method links must preserve 17 regression-discontinuity sites and one randomized site; do not label its entire impact study randomized. Next: finish concept alias/type review and choose relation semantics with explicit site-level scope, then publish and verify the method mapping.

Reading First's imported context is now preserved on the existing program. Proposal `da82f1849e724228b7cb80c26b92264a` executed and its bounty link indexed; all seven operation checks pass. Main transaction `0x8cf00708760382749c95ca3e2594addaef0c0a11087e3a578def096b69b9a991`, bounty transaction `0xb858fc6c3ca6da7112ea39566987fa02789f7652a5c9c496fc791ea38cd8982d`, vote `0x1bcdc778c14015b0198c806d2b4eb7d2700e718ff63afafd56ff1aed7d6a44e6`.

Browser verification confirms all 17 labeled fields, the editorial/method caveats, pinned provenance link and Route slug value render on the program page. Evidence: `reading-first-import-context-browser-verification.json`. The reconciliation deliberately remains incomplete: preserving a source field as context does not create the structured relations needed for filters.

Reused cross-space Text property Route slug `b0305ef28312c519d954bc0efe22f013` now stores `reading-first`. Its live definition was inspected, and the program had no destination slug before the write. Typed Text block `c9bc9c2dbc4247469bf40b4a0b6738df` preserves all 17 original source fields with the pinned repository link and explicit editorial/mixed-design caveats. The audit now reports `preservedInImportedContext` separately from semantic `complete`; all 17 source values are present, while only Name, source references and route slug are marked complete. Source preservation must not be mistaken for finished component, method, evidence-rubric or comparison relations. This supersedes older statements that the slug is only local. Typecheck passes.

`scripts/education-reconcile-reading-first-record.ts` checks every one of the 17 fields in initiative 12 against explicit mapping decisions and saves the original values in `data/education/reading-first-import-reconciliation.json`. A newly added source field fails the audit until reviewed. Six live identity/reference checks pass: Name, impact source, background source, study, dataset and administrator. Only the name and original source-reference fields are marked complete; partial prose coverage does not count as complete structured migration.

The current record includes Children/K–3 and OESE relations, superseding older unresolved audience/administrator notes below. Missing work includes import identifiers, historical-period scope, Literacy/category and intervention components, method relations, editorial evidence-classification rubric, and the composite debate link. The imported years are not precise dates; RCT must not mislabel the whole mixed-design study. See `docs/reading-first-administration.md` for executed, bounty-linked administration evidence.

Taxonomy discovery found existing Reading comprehension Topic `5c028b0a8a484ee9ae29e07028a1e7d1` in Podcasts space `b5a31f8182b042437ede0f84ee02f104`; inspect its identity further before use. Exact Literacy, Phonics and Reading instruction searches returned no matches; these alone do not settle creation. Grant program schema exposes Focus areas `02d0b9117c4f119d4b986ded266d7028`, but its live datatype/range must be checked before choosing structured topic links.

## Reading First structured evaluation designs — 2026-09-12 UTC

Follow-up verification: `education-verify-reading-first-methods.ts` passes six live cases: each individual design component, both components together, exclusion of the randomized component, and each relation entity's exact scope description plus report citation. Results are in `reading-first-methods-query-verification.json`. These are bounded Reading First checks, not a graph-wide methods audit. Browser tab 63 confirms both method links render alongside the existing “17 regression-discontinuity sites and one group-randomized site” text. The default property list does not expose the per-relation scope descriptions inline; a custom dashboard must fetch them using each edge's `entityId`. Do not use the edge ID for that lookup. The canonical page route is `/space/{spaceId}/{entityId}`; inserting `/entity/` produced a 404. This supersedes the browser/filter pending note below; dashboard implementation remains unverified.

Proposal `0d89165a041046499c9599bb57e351a6` executed and its Education bounty link indexed. `reading-first-methods-index-verification.json` records 12 passing operation checks; `bun run typecheck` passes. Main transaction: `0x60d41a03f44bdebd57123b6f5ec87ebdfede94851f5f2a1bbbbd332755c54e2a`; bounty transaction: `0xb9a71dcf9f5121b581454a8566808a89f363fc53fe6d4e9919146725f4701612`.

Completed alias discovery (`regression-discontinuity-alias-discovery.json`) found only unrelated discontinuity/RDD candidates. Created Topic `b5e82046c5694090aeb0c83f7a0208f9` for Regression discontinuity design and reused existing RCT Topic `ad9f48f68b804ddd9f9e8ffa5869ef02`. Study `d87bc8f30fb84b5fa660f98d1e770c78` now links to both through Relation-valued Study design `b1fdc75afb6841a18d8286427099ea44`. This supersedes the alias-review and method-publication pending statements above.

Scope descriptions and citations are attached to each relation's **entity ID**, as required by the SDK data model. They preserve 17 regression-discontinuity sites and one randomized site. Keep the existing mixed-design narrative. A dashboard filter matching either relation means the study contains that design component; it must not classify the entire study as randomized. Do not reuse Health's intervention-delivery Methods property for research design. Browser presentation and independent dashboard filter verification remain pending; this does not complete the imported method record or the full initiative migration.

## Reading First program published — 2026-09-11

Background-reference follow-up: `src-014` now maps to Article `bb326cc9809b4bf89d161701f49b74d9`, the National Reading Panel's full subgroup report (April 2000, NIH 00-4754). The program's Sources relation includes it, and a labeled background section distinguishes this research synthesis from the Reading First funding evaluation. Source correction preserves the imported topic-summary URL and original generic finding in intake `data`; `publicationData` uses verified report metadata and scope. Intake still reconciles 339 records and 383 references. No panel effect estimates were invented or added to the Reading First impact dataset.

Proposal `17bc0e5129434d5488e1c264e6229af8` executed and is bounty-linked; all 12 index checks pass. The source page renders its concise description, publication/scope block and three official links. Curator shows this proposal and program proposal `3e733398ff2c408caaab6bfca310ae5f` Accepted: 26 accepted rows total, submission In progress, no payout. Evidence: `reading-first-background-index-verification.json`, `reading-first-background-presentation.json`. This supersedes the program proposal's Curator-pending note below.

Administration discovery: root Administered by `d1c6034425684b4caaf2570bee562802` is a candidate relation property, with a definition appropriate to administration rather than local delivery. Its live datatype/range and actual uses still need checking. Exact and substring Department of Education searches and alternate spellings found articles/claims but no matched agency identity; discovery reports retain the candidates. Do not use a news article as the administering organization. Program target population and study sample remain separate unresolved mappings.

Program `26c06d8c6d1d42f2b87bbf3a51806a18` is published in Education datasets with the existing Grant program type `9ba04d040e6bed81dc2ebd593e8cd0e7`, reused from space `d69608290513c2a91102c939b3265bd7`. Its complete schema and bounded instance samples support the funding-program interpretation. A new Education program type was unnecessary. Root Project candidates instead represent task-managed work or companies/products; the imported Initiative type has a different, weakly documented schema.

The program links bidirectionally to the existing study and dataset through Related entities, and links to the existing source report and United States. These are program-context associations, not yet dedicated local-implementation relationships. Its two-sentence description and separate source-backed context block render, along with all source/study/dataset/location links and backlinks. Evidence: `reading-first-program-index-verification.json` (16 passing checks) and `reading-first-program-visual.json`.

Proposal `3e733398ff2c408caaab6bfca310ae5f` executed and its bounty link is indexed. Transaction and CID evidence are in `reading-first-program-publication.json`. Curator presentation of this newest proposal remains pending. The full imported initiative record, second source reference, operator, target population, categories and implementation model remain unfinished; do not count this as complete migration of source record 12.

Discovery lesson: broad all-type substring searches for program and initiative reached the 200-candidate cap and remain explicitly incomplete. Separate complete Type-only ontology searches found Grant program and Initiative; these resolve schema candidates, not program identity. Program identity used the complete specific Reading First substring/alias and legacy-URL searches. No failed search was treated as proof of absence.

## Source review — 2026-09-11

The bounty requires searchable programs, their operators, populations, geography, mechanisms and implementation models. Published effect rows alone do not establish these connections. Preserve all 76 imported initiative records and their references, but do not assume each record denotes exactly one real-world program.

Review of `data/education/intake.json` at pinned source commit `3cd97449ce9ca73cccb77efe22aac69cc56131e5` identifies these boundaries:

| Imported initiative | Identity decision required |
| --- | --- |
| 12: Reading First | Federal funding program is distinct from its national impact study, funded/comparison study arms and published results. Preserve imported years as source text until exact dates are verified. |
| 20: Saga-style and tech-infused tutoring | Broad intervention family is not identical to Saga Education, a specific Chicago trial, or software's separate contribution. Link the current Saga findings only to the matching implementation. |
| 21: Project STAR / class-size reduction | Composite record combines a named experiment with the broader class-size mechanism. Its modeled economic scenarios and measured experimental effects remain distinct. |
| 43: Head Start | Federal program, individual operators and national study/cohorts need separate identities; other preschool access in comparison conditions matters. |
| 44: Perry Preschool / Abecedarian-style intensive early childhood | Composite of distinct experiments and intervention models. Do not merge Perry and Abecedarian IDs or apply one trial's costs/outcomes to the other. Preserve this imported grouping as a curated overview linked to separately identified programs. |

## Live discovery evidence

All-space/all-type exact-name searches for Education program, Program, Intervention, Implementation and Education initiative completed on 2026-09-11. No exact Education program, Program or Education initiative candidate was returned. This is not proof that no suitable type exists under another name.

Four Intervention candidates include two Properties, a Proposal and an untyped entity; none is a verified reusable program Type. Implementation `6e528b43e6024e6c8708fdd0bdd2b30c` is a Topic describing a New York property-tax proposal, not a general implementation type. Reject it for this purpose. A complete Reading First substring search returned 76 candidates, including the published study material; inspect identities and aliases before claiming program absence. Reports are in `data/education/discovery/`; prefer hashed filenames.

Next: inspect broader program/project/policy type synonyms and their schemas; inspect cross-space Reading First candidates and primary-source program context. Resolve population, location, operator and implementation relationships before publishing the first program-to-study connection. No program entity or program relationship has been created by this preparation.
