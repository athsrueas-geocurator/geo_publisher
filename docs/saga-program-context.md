# Saga program, provider and trial identity

## Pooled and later follow-up extraction — September 11, 2026

`saga-pooled-transcription.json` and `education-prepare-saga-pooled.py` now cover all 19 source rows of main Tables 5 and 7: 152 independently compared numeric cells, yielding 38 ITT/TOT representations. Tables were inspected in `tmp/pdfs/saga-followup-14.png` and `saga-followup-16.png`; the fingerprint-checked PDF text supplies the independent numeric comparison. `saga-pooled-extraction.json` is verified preparation, **not published data**. Publication still requires pooled-analysis identity/reuse decisions, source-specific Claim text, schema checks, bounty linkage and live read/render verification.

An initial visual transcription confused the violent-arrest ITT coefficient with its SE. The validator rejected -0.011 and confirmed the correct coefficient **-0.012**, SE **0.011**, in Table 5. The transcription was corrected and all 152 cells then passed. Nothing was published from the failed transcription; the earlier verbal suggestion that -0.011 was the coefficient was incorrect. Preserve this as a reason to compare every numeric column independently rather than trust small rendered-table text.

Table 5 pools both existing studies in the first postrandomization year. Table 7 also pools them; neither is a third independent randomized trial. Math outcomes in Table 7 refer to the year each student would reach eleventh grade absent retention, not one common elapsed follow-up year. Source p.752 describes persistent math gains and imprecise graduation estimates. On-time graduation TOT is 0.013 with SE 0.032 on the fraction scale; this does not establish improved graduation. “Graduated ever” means within the observed data window, whose terminal date remains unresolved; it must not be called lifetime graduation.

Table 6 remains separate required work: its first outcome estimate is a weighted per-year effect; the next columns are upper bounds on persistent first-year and second-year effects under assumptions described on pp.751–752. They are neither ordinary independent treatment effects nor confidence bounds. Years of tutoring were not separately randomized. FDR q-values in these tables remain shared outcome-family statistics, not invented per-estimand p-values. Suspension units and control/complier-mean modeling remain unresolved.

Claim-readability correction, 2026-09-11: the 58 new outcome Claims' original row-label names and generic descriptions were inadequate on dedicated Claim pages. All 58 now state source-backed findings with exact estimate/SE/N/locator and a concise paper interpretation, using the same IDs and numeric values. Repair `eed34163f96a4908b0095f35ee52b184` executed and is bounty-linked; 118 checks pass. The user-reported property-arrest Claim body now renders the correction. See `education-description-guidelines.md` and `saga-claim-copy-browser-verification.json`. Original publication batches are historical snapshots; this repair supersedes their name/description expectations.

The older context-filter verifier is updated for the expanded 32/30 first-year memberships; all eight provider/city/trial/population cases pass again. Its original four-math-effect numeric checks remain alongside the comprehensive outcome verifier. This supersedes the historical two-effects-per-study warning below.

## Cost accounting correction and broader outcomes — 2026-09-11

Curator verification: all four new proposals (cost accounting, labels, outcome pilot and expansion) appear Accepted among 36 linked proposals. The user's submission remains In progress with no payout. This supersedes any Curator-pending wording below. The frontend task received the outcome crosswalk, tested query, explicit unknown-unit restrictions and source-label distinction. These checks do not complete the bounty or full migration.

The stale cost title and program-context sentence are now corrected by executed, bounty-linked proposal `addd37f761da4b41a17991b3ce6681b1`. All four checks in `saga-cost-labels-index-verification.json` pass; the fresh dataset browser page shows both corrections. Main transaction `0x41c1fda3254a0afca46f650f1e78fa0960d833801e28e48493d95b4628b300d7`; bounty transaction `0x0081be80576b383dabe4723cf00c0a33a4081b5adb68459b3326a627eee27142`. This supersedes the stale-label follow-up below.

All **62 first-year ITT/TOT estimates** from Tables 3–4 are now published: the four original math Claims are reused and 58 new Claims cover GPA, failure rates, additional math/reading tests, attendance, discipline and arrests. Pilot `2832bbba68204441ac33376c4d556f8d` passes 33 publication checks and rendered GPA verification. Expansion `d5376b35f56d494d9b6d9ee6b5dd723b` passes 1,081 publication checks across 70 affected entities, including execution and bounty linkage. Its main transaction is `0xc1fa4a43627c61df8223a8b2d9d05629b9e8e65379f2303891d0445d31b85ba3`, bounty transaction `0xee4431a9bba1ec64a1ee6aa7054f9689603ef6b2b0b481a231a7de4323f17cd6`, and execution vote `0x3319d07d7a82c98857485e08795b0ef05c4ad6ed3115a1ff1835c40667785edc`. Saved batches/journals remain immutable.

All 23 live query cases pass (2,503 assertions) in `saga-outcomes-query-verification.json`. The verifier uses the original source transcription rather than publication operations for expected values. It checks 62 total effects, 32/30 by trial, 31/31 by estimand, every exact outcome label, source/own-trial/population connections, reported N, exact effect/SE, and suspension unit absence. `saga-outcomes-browser-verification.json` records all nine table pages inspected: mathematics 14 rows, nonmath 12, attendance/discipline 12, arrests 24. The original four-row math summary is a second view of reused IDs, not four extra observations. The older context-query verifier's two-effects-per-study expectation is now historical; use the expanded outcome query for current membership.

Four domain tables use existing numeric and relation properties. `saga-outcomes-registry.json` maps every source cell key to its stable ID. The dataset's former single standard-deviation unit is corrected to “varies by outcome; see individual estimates.” Four suspension estimates deliberately omit the unit, with unresolved days/events meaning stated in their descriptions and the dataset methods block. This preserves reported values without fabricating comparability. Shared control means, estimated control-complier means and FDR q-values remain unstructured pending semantic mapping; later-year and pooled results remain unfinished. Published source labels differ for the two CPS math tests, so exact Outcome measure text filters can return only one trial; cross-trial instrument concept mapping is still needed.

Fresh all-space Saga-name discovery found 167 candidates; unrelated SAGA/SITE statin results, places, books and crypto records were excluded. `saga-outcome-link-discovery.json` additionally paginates all incoming relation types across all spaces to the exact final article and both trials. It found only the known study/program/dataset/cost and four prior estimates before publication. New-entity exact-name probes and complete pages are preserved in each outcome batch's identity report.

The journal's official [replication package](https://doi.org/10.3886/E182903V1) identifies `scripts_r/exhibit_scripts/main_effects_tables.R` and `adding_fdr_qvals.R`. Public directory/file metadata is readable. Download links redirect to ICPSR login; the public preview opened a blank frame. Script contents were not inspected, so these filenames do not resolve suspension units or establish which estimand underlies each q-value. No login or data-access claim is inferred.

The official [online appendix](https://www.aeaweb.org/content/file?id=18262), section V (A-6–A-7) and Tables 17–18 (A-24), clarifies that the approximately USD 3,500 headline uses nominal proposed budgets per available treatment slot across the two program years, excluding sunk planning costs. The USD 3,200–4,800 range mixes the second-year per-slot denominator with the first-year per-participant denominator; it is not sampling uncertainty. A common constant-dollar price year is not supplied. The later technology-assisted model and subsidy are not the original trial's cost. Appendix SHA-256: `de529eeb3f45251e1a78e8ea531bd5f4a35c17267ccaf85297128d271702b57a`.

Proposal `3b39e7bbd98741c998ce10a2f55f5182` executed the three-operation correction to the existing cost Claim and Text block and attached that block to the dataset. All seven checks pass in `data/education/saga-cost-accounting-index-verification.json`, including execution and the indexed bounty link. Main transaction `0x3f3444dcf549ddedecb7bbd02dd2cbd430191ef9e66ce390945d84e22eedd00d`; bounty transaction `0x5c80a7cb819623ce7bb6a6eb44ca81252ce23959f61d20d5a6018759451a973e`; vote `0x06c7e49a7d40807a52dfb2cae2bde36683512af0235c63bd657392053592db10`. Numeric values 3500/3200/4800 were preserved. A fresh Geo browser tab rendered Budget basis, Why the range differs, Comparison limits and source links beside the existing four-effect table. Curator acceptance display has not yet been checked for this proposal.

Browser review found remaining stale presentation: the cost title still says “per pupil,” and the program context block calls the price year unverified. Update these to reflect the verified nominal-budget basis; do not modify the immutable submitted batch. The new block currently appears before the existing program context and table; final comparison-page ordering remains to be reviewed.

Broader outcome preparation is in `data/education/saga-outcomes-transcription.json` and `saga-outcomes-extraction.json`, with `scripts/education-prepare-saga-outcomes.py`. All 31 source rows / 248 numeric cells from final Tables 3–4 were independently compared against the PDF. They supply 62 ITT/TOT estimates, including the four existing math estimates; the other 58 are not published. Preserve nonmath, attendance, disciplinary and arrest outcomes alongside math. Suspension units remain unresolved for four estimates. Course-failure values are fractions despite percent labels. Shared row-level FDR q-values are not ordinary per-estimand p-values, and estimated control-complier means must not be represented as observed control means. These modeling questions and later-year outcomes remain unfinished.

## Primary-source findings — 2026-09-11

The [2023 final AER article](https://educationlab.uchicago.edu/wp-content/uploads/sites/3/2023/10/UChicago-Education-Lab-Not-Too-Late-Paper_03.23.pdf), printed pp. 740–743, distinguishes a tutoring service, its developer/provider lineage and two randomized studies. The studied delivery was in-person, 50 minutes each school day with two students per tutor; later blended-learning models are different variants. The paper's footnote 6 explains Match origins, SAGA Innovations and the 2019 Saga Education name change. The current [official organization site](https://saga.org/) uses Saga Education. Its 2023 annual-report search result confirms nonprofit spin-off in 2014; direct page fetching returned 403, so do not infer additional current operational facts from inaccessible page contents.

Study 1 randomized male ninth/tenth-grade students in 12 CPS high schools before 2013–2014 and independently assigned BAM. Study 2 included female and male students in 15 schools in 2014–2015, without BAM factorial assignment. Study 2 had 2,645 distinct students and 2,710 records because 65 students were randomized twice; footnote 10 explicitly says to cluster by student. These totals are not the outcome-complete N on the already published math estimates. The different populations and exposure offers must remain visible.

## Dashboard query boundary — 2026-09-12

The broad comparison-dashboard relation filter is not yet valid for Saga. Direct reads confirm first-year Claims retain their intended Study and Article edges, but the paginated Article-plus-Study query returns a mixed set of later/pooled records while omitting checked first-year IDs. Do not add replacement edges or claim the records are absent. Use the verified table-specific query contracts for Tables 3–4, 5/7, 6, and Appendix Table 5; every consumer must filter by table/follow-up and estimand before comparing records. Reproduction details are in `docs/geo-api-diagnosis.md`.

## Live ontology and identity discovery

All-space name search for Saga completed with 157 candidates. The same-name cities, cryptocurrency project/token and book series are unrelated; existing education results are our dataset, four claims and cost, not the provider or separate Study entities. Complete all-value alias searches for Saga Education and SAGA Innovations returned zero. Host identifier searches for sagaeducation.org and saga.org returned zero. The existing paper and all outcome IDs will be reused.

Root Service `2f3e568ca8cb4d6ea130829c3012648f` has a broad activity-delivery definition, with sampled data/authentication services consistent with it. It fits the tutoring delivery model without inventing an Education program type. Root Organization `9547f4fb78744de0a9a9fdd7b4c01c0c` and root Project `484a18c5030a499cb0f2ef588ff16d50` represent provider organizational/project facets. Providers `261fad421cc744938acbaa4a4c74220c` is Relation, with Project range; actual uses include services/courses/datasets linked to institutional providers. The provider is therefore typed Organization + Project, following the ontology's composable institutional facets. The service is not a grant program, and provider location must not be inferred from the trial city. Study `3ef269bc5f114691abc02dcbf398fd63` is reused for the two trial records.

`saga-context-inspection.json` contains complete root schemas, deliberately bounded type/provider examples, property datatypes/ranges and complete alias probes. Bounded examples are semantic samples, not exhaustive absence checks. `saga-context-model.json` specifies source-backed descriptions, population/design text and labeled context blocks. Providers, citations and study-specific facts extend the types' suggested schema; no new primitive or property is proposed. Grade entity mapping and dedicated cohort relations remain unfinished.

## Pooled extraction preserves shared source statistics

### Table 6: distinct statistical quantities

**All 15 Table 6 statistics are applied.** Expansion proposal `3b3b30248a174b8780cedaec05187bf2` executed: main transaction `0xa07b3f8cbc2835fa6bff1bf4556f6ff0a0404fb55953aca487874e569ef613cd`, bounty transaction `0xa7472936f1be62c5ec71a6bf15564aaad7bcc7debd48309c199db44f827d80df`, vote `0xfd8e2039771d6631fe27aaad0f52debda60c878a1e7665e8e155383ba45931d1`. The 14-record expansion passes 276 operation checks including execution and indexed bounty; combined with the pilot, all seven source rows are represented.

Browser verification completed for all four Table 6 tables (3/4/4/4 rows, no pagination). The seven effect records and eight bound records display under their distinct value columns, with correct signed numbers, SE, N, units and estimands. Course-failure titles express reduction magnitudes as percentage-point display conversions while stored bounds remain -0.109 and -0.281. `saga-year2-browser-verification.json` records the scope. Full frontend comparison exclusions and current Curator presentation remain separate requirements.

`education-verify-saga-year2-query.ts` reuses the paginated table query and independently reconciles source transcription cells to all 15 live records, passing 199 checks. It verifies the four statistic groups, source-signed values, SE/N, specification-specific q text, own Study 1/source links, and qualified estimands. Eight bounds omit ordinary Effect estimate value and both confidence endpoint properties; the seven remaining records comprise three participation first stages and four weighted per-year effects. Thus effect-property presence alone still does not select comparable academic effects: consumers must filter Estimand too. Stable IDs are in `saga-year2-registry.json`; new bound property `f44acacc373c4c12a10bec95ad915ed6` is live Decimal. Typecheck passes. These publication reports supersede historical preparation labels below.

Bound-property pilot is applied: proposal `662f8ee1c7824e69a969f5db612ce008`, main transaction `0x53865755697865629f883915b2fcf5c7a8886f7da8d831b8e8932a72e6d49c44`, bounty transaction `0xd2e72a4e8fc5c428523e57dfc330ad9485a2e6c8bf9e3c6705e6e1dc805fba57`, vote `0xa4efcdfb95ad5b41fd45c5ecd77e466c508db5c80dc6edd9702265070af2c2b6`. All 39 checks pass including execution and indexed bounty linkage. New Decimal property `f44acacc373c4c12a10bec95ad915ed6` is Assumption-dependent bound value. Pilot Claim `8e40fee2cbc74c70a468b2493aabc544` stores 0.305 there, with SE 0.067, N 1640 and no ordinary effect or CI endpoint values. Its dedicated page and table render the bound, assumptions and citation correctly; evidence `saga-year2-pilot-browser-verification.json`. Fourteen other statistics remain pending at this stage.

Live property discovery (`education-discover-statistic-properties.ts`) completed ten all-space/all-type exact-name searches and a cursor-paginated search of 695 names containing “bound.” The existing Upper bound `2f61be01ee135b66a9bcb50770f93631` and Lower bound `b3adc5f5edc651a5a815f9edc8b546e8` are Float properties for metabolic reaction flux in space `aec359295ff90d00aea6696f0ddb1bbc`, not general statistical bounds. Existing education cost and confidence-interval bounds likewise have incompatible meanings. Reusing a name without reading its source-space definition would corrupt the model.

`saga-year2-model.json` therefore proposes a Decimal Assumption-dependent bound value property for the eight bound records, while retaining the existing Effect estimate value for the three participation first stages and four weighted per-year effects. All four categories reuse Estimand `ba643714da274c37a340a7f8396198dc` with explicit quantity labels. Bounds must omit ordinary effect/CI endpoint properties, and participation first stages must be excluded from academic-outcome comparisons. This is a model, not a published schema change; final proposed-name/semantic checks, property creation, content publication, bounty linkage and live checks remain.

`saga-year2-transcription.json` now preserves all seven Table 6 source rows on printed p. 753/PDF p. 16. The independent extractor `education-prepare-saga-year2.py` matches all 53 numeric cells against the fingerprinted PDF, producing 15 statistical records: three first-stage participation effects, four weighted per-year outcome effects and eight assumption-dependent bounds. The original page was visually reviewed. These records remain unpublished pending semantic mapping and publication verification.

Unlike Tables 3–5/7, Table 6 prints a separate q-value for each Panel B specification; some differ within a row (for example GPA 0.012/0.012/0.013). The extractor therefore retains each specification's reported q rather than propagating one shared row statistic. All remain FDR-adjusted values, not ordinary p-values. This corrects any overbroad interpretation of earlier notes that treated all Saga q-values as shared across specifications.

The bound columns must not use confidence-interval endpoint properties or enter ordinary point-effect rankings. The course-failure estimates are negative, including reported bounds -0.109 and -0.281; the beneficial-effect direction differs from higher test scores. Preserve those source signs and explain the authors' bound construction rather than labeling a negative bound an ordinary upper numeric endpoint. Main-text pp. 751–752 supplies the nonnegative-benefit assumptions and weighted mixture interpretation. No local recomputation is substituted for printed values.

### Study-specific later outcomes: source preparation

**Applied and verified:** the same proposal subsequently indexed without resubmission. `saga-later-index-verification.json` passes all 308 checks, including execution and bounty linkage. `saga-later-query-verification.json` independently reconciles all 16 records to the appendix transcription over four cursor pages, passing 228 checks including exact values, own-trial-only membership, counterpart links, factual flags, citations and qualified follow-up. Typecheck passes. This supersedes preparation/unpublished labels below and in historical extraction files.

Browser verification inspected both table pages (nine and seven rows) and the dedicated Study 2 math ITT Claim `e52d2e100edf4e54954ee9b76480c094`. Its description visibly preserves q=0.502 and the lack of an established later math improvement, alongside 0.027 SD, SE 0.040, N 1445 and the A-12 citation. See `saga-later-browser-verification.json`. Table ID `83c9c6a366e14992a951ec702dbc67a3`; stable Claim IDs in `saga-later-registry.json`. Main Table 6 and remaining program/cost/measurement mappings are still unfinished.

Publication submitted and executed as proposal `7b86abc860204e8db12bfd3f95070da2`: main transaction `0x652020f0ff30bfcbe9519248afcb2ce712b6df30f2e802475d8911acf12ffab4`, bounty transaction `0xae72ae27faec6681d19e939b8fa4c76bdda1c1d23fc0282aa0ba4ca5309b263f`, vote `0x4e9ec01b12b41c337db0fc4fdcdf6f85e9bd27b738618e276db438b611db41af`. The first immediate verification found the new table absent and therefore failed; chain execution alone does not establish indexing. Recheck this same proposal rather than resubmitting. Stable IDs and immutable operations are in the `saga-later-*` artifacts.

The final source-neighborhood discovery completed 14 pages (106 incoming Article relations, 72 Study 1 and 70 Study 2). All source-linked Claims matched prior estimate registries except the existing nominal budget-cost Claim `0adff420a6bc480b8973880dfdb5f296`; its reviewed cost identity was explicitly excluded from later-outcome duplication concerns. The new builder checks all-space Saga names, exact new names, live property datatypes, existing types and unused persisted IDs. Its value/column layout reuses the already browser-verified pooled later-outcome schema plus the existing first-year Study number field; Name is left implicit. Source-specific text for all 16 Claims was reviewed before submission. Typecheck passes.

Follow-up review found an explicit connection: main-paper p. 752 footnote 17 says the separate Study 1/2 cohort results are in Appendix Table 5. The main text describes expected eleventh grade without retention, while the appendix notes possible multiple eleventh-grade years. This establishes the intended cohort-specific follow-up connection, but does not resolve repeated-grade record handling. The publication model therefore uses “Eleventh-grade outcome as reported in Appendix Table 5,” retains the ambiguity in a methods block, and does not fabricate elapsed years. This permits publication of verified findings with an explicit limit, rather than treating an unresolved detail as grounds to omit them. `saga-later-model.json` records the exact wording and reused IDs.

Rendered Appendix Table 5 on PDF page 13 and checked its eight rows against the fingerprinted official appendix. The printed locator is **A-12**, correcting the earlier working assumption A-11. `saga-later-transcription.json` preserves all eight numeric columns; `education-prepare-saga-later.py` matches rows within each study panel because outcome labels repeat. All 64 numeric cells pass, producing 16 ITT/TOT representations in `saga-later-extraction.json`. These additional study-specific results are not yet published.

Study 1 reports later math-test ITT 0.159 (SE 0.039), TOT 0.304 (SE 0.074), row q 0.001; math GPA ITT 0.132 (SE 0.053), TOT 0.250 (SE 0.100), q 0.013. Study 2 reports math-test ITT 0.027 (SE 0.040), TOT 0.082 (SE 0.122), q 0.502; math GPA ITT 0.082 (SE 0.053), TOT 0.242 (SE 0.156), q 0.121. Different within-study significance results do not establish a statistically significant between-study difference. Preserve the pooled estimates separately; do not impose their interpretation on every trial-specific result.

The appendix explicitly says students may have multiple eleventh-grade years and retains duplicate Study 2 randomizations with SE clustered by individual. Resolve the observation-window meaning before copying the main table's expected-grade wording into these Claims. Graduation-ever terminal date also remains unresolved. Reuse each existing trial ID; link the correct trial only, with reciprocal alternate-estimand links and the exact appendix locator. These 16 representations add no independent study.

**Tables 5 and 7 are now published.** Expansion proposal `84cde81d50d543c6a679a974f5f13197` adds the remaining 37 Claims; combined with the pilot this supplies 38 representations of 19 source contrasts. Main transaction `0xcae377061125260e77a56811627b7a19f71b59e575798ed9397cbf049e6b2321`, bounty transaction `0x79ad12a7cea21f2043b1070390972608218ead21cae5225d7ee55845b9353a26`, vote `0xd6563a0eb6ed407c32852aab29ffea2008a2c558627e75879c9bce8302735875`. All 680 expansion checks pass, including execution and indexed bounty relation. The source preparation/model files retain their historical preparation labels; the publication journals and verification reports establish current applied state.

Browser follow-up inspected every pooled table page: Table 5 has 9/9/9/3 rows; Table 7 has eight rows on one page. Both tables show one Name column and the expected typed numbers, uncertainties, sample sizes, units, estimands, follow-up labels and locators. Suspension units remain blank, and both Graduated ever estimates display zero with nonzero SE. `saga-pooled-browser-verification.json` records this scope. Dedicated Claim readability was separately checked on the pilot. Current Curator presentation and full frontend integration remain separate checks.

`saga-pooled-query.graphql` reads the two tables through five-edge cursor pages with destination-scoped Claim facts. `education-verify-saga-pooled-query.ts` reconciles the live records directly to the source transcription, independently of operation payloads. All 467 checks pass: 30 first-year records across six pages, eight later records across two pages, exact effect/SE/N, outcome labels, estimands, known/absent units, factual flags, original citations, both existing trial links and reciprocal alternate-estimand links. The union remains two underlying trials. Stable IDs are in `saga-pooled-registry.json`. No third Study or new metric primitive was created. Typecheck passes. The frontend task received the query, IDs and interpretation constraints; this does not prove full dashboard implementation.

Broad all-space Saga discovery also found our own pilot Proposal; the first expansion dry-run stopped for review. Its exact journaled ID identified it as the known publication record, not a duplicate scientific Claim. The builder excludes only those reviewed pilot/repair proposal IDs and pilot-created IDs, preserving the candidate gate for unknown matches. No failed or partial search was interpreted as absence.

The pooled pilot repeated a known table-layout mistake: explicitly configuring Name produced two Name columns because Geo supplies it automatically. The original `education-fix-saga-column.ts` already demonstrates the correction; the pooled repair reuses that pattern with an exact live edge/space/endpoint check. Repair proposal `be812d81a6654b6983995a30de511363` removed only that redundant display relation, executed, and passed all three checks including bounty linkage. A fresh browser view shows one Name column and the correct typed pilot values. The builder now omits Name from future column metadata. Evidence: `saga-pooled-pilot-browser-verification.json` and `saga-pooled-column-publication.json`. Historical pilot operations remain immutable; their removed-column assertion is superseded by this repair.

Publication preparation exposed a verifier limit: `education-verify-saga.ts` previously rejected any entity with more than 100 values or relations instead of paging it. The expanded Saga Dataset crosses that relation count. The verifier now reads destination-scoped values and relations in separate 50-row cursor pages and rejects repeated or missing continuation cursors. Explicit `any` annotations on the dynamic GraphQL result/page resolved TypeScript TS7022 errors introduced by the change. Typecheck passes; the existing Reading First Topics batch still passes all eight checks, and the new Saga pooled pilot passes all 37 checks.

Pooled pilot proposal `ceed31cabf5b463a880faf765b537a48` executed. Its main transaction is `0x636938a818d590da7bb586ba5a5a23e8451d35fd9c07f4d184b2a0f142323815`, bounty transaction `0xb2759901ff9d518c79d7c7395d99bf0e191448a17313061054337572243c0346`, and execution vote `0xb258c2628cf44be6fa1d87e26daee5e6dc6095dfc2d59bfda60ab35dc0b65b12`. Claim `f80ce56fb6274f829b8f762a717165b7` displays the actual 0.119-SD offer effect, SE 0.025, N 3717, Table 5 locator, reported row q-value, Verify/Dispute controls and original Article link in Geo's dedicated Claim page. The remaining 37 estimates are not yet covered by that pilot evidence.

The subsequent live identity check (`scripts/education-discover-saga-pooled.ts`) completed eight cursor pages across all spaces: 68 incoming Article relations, 34 Study 1 relations and 32 Study 2 relations, representing 68 unique neighboring entities. No neighbor name/description matched the follow-up screening terms; this is a complete linked-neighborhood search, not graph-wide absence proof. Evidence is saved in `data/education/saga-pooled-link-discovery.json`; broad name/alias discovery and exact candidate review still apply before allocating new Claims.

The preparation model in `data/education/saga-pooled-model.json` reuses the existing Dataset, Article and both Study IDs. Pooled Claims will link to both trials and omit the single-study integer, rather than inventing Study 3. The two proposed tables contain 30 first-year and eight later representations of 19 source contrasts. This model is not a submitted payload; concise source-specific Claim copy, live schema validation and final identity review remain prerequisites. Dashboard verification must explicitly test both study links and prevent pooled/ITT/TOT representations from increasing independent-study counts.

On September 11, 2026, review of `education-prepare-saga-pooled.py` found that it validated all eight numeric columns but emitted only the per-estimand effect, standard error and N. The extraction now also preserves all 19 source rows, including control means, estimated control-complier means and reported FDR q-values. Each of the 38 ITT/TOT records links to its shared source row through `sourceRowKey`; duplicate keys, missing row links and nonpositive standard errors fail validation. Rerunning against the fingerprinted final paper passed all 152 numeric-cell comparisons.

This follows the existing first-year Saga extractor's row-level preservation logic. It prevents downstream preparation from dropping verified statistics or treating the row's FDR q-value as two separate ordinary p-values. These are local preparation artifacts, not proof of Geo publication: pooled identity discovery, semantic mapping, Claim copy, bounty-linked publication and live dashboard checks remain unfinished.

## Identifier evidence filename correction

`education-identifiers.mjs` formerly keyed manual report filenames by argument index and URL pathname, so different homepages could both overwrite `manual-0-.json`. The tool now always writes a full-URL-list hash filename and updates legacy names only if the URL list matches. Use the hashed evidence paths; inspect probe URLs rather than trusting a legacy filename. Historical overwritten results cannot be assumed recoverable. Saga homepage probes were rerun as `manual-0--370e3c21c20be17e.json` and `manual-1--ed47576a8435474f.json`. This fixes evidence identity, not graph identity.

## Published and verified

Curator follow-up: the Saga context proposal is Accepted among 32 linked proposals; the user's submission remains In progress with no payout. Evidence: `saga-context-browser-verification.json`. This supersedes the Curator-pending statement below. The frontend task received the new IDs, tested queries and interpretation limits; this is a handoff, not proof of frontend implementation.

Proposal `6b8f9922d32e4c7cb1ad894b11055526` executed and its bounty link is indexed. Main transaction `0xe098a10d3dbf4ce81d492f54b639fcb140d9e1e62ea45f86998b672757c11699`; bounty transaction `0xc41ecb7bb42eec6b81b72a707e055c4588f405236193e7931f5f26bbd7ecd562`; execution vote `0xfa086401af3b7d8eaaf6c7d86779a254f08af0096813140846af9095c88e782f`. All 62 publication checks pass across 13 affected entities. The 42 operations added seven typed context entities and links, without modifying existing cost or effect values.

Provider `bfac9c728a0e43fd9a278a288d9474a0`; service/program `6ce956de8cc047178c33046dd525009d`; Study 1 `6ff69ada18b54b1db98375da1b90df65`; Study 2 `86c407ce9e7e49819e1cd8d07b20f4c8`. Each existing math Claim now links to its own Study through Related entities; the shared cost links to the studied program without assigning a subgroup or study-specific cost. Source and Chicago IDs are reused.

All eight [live query cases](../scripts/education-verify-saga-context-filters.ts) pass: provider/city service lookup, wrong-provider empty result, both program studies, each population, each study's two effects and no effect assigned to both studies. The verifier independently checks the original effect and SE values against the extraction. [Saved query](../data/education/saga-context-query.graphql); report `saga-context-query-verification.json`. TypeScript compilation passes.

Fresh Geo browser tabs render the service description, delivery/history block, Providers → Saga Education, Chicago, both study links and source; Study 2 renders the distinct-student/randomization-record explanation with its citation. Curator presentation for this newest proposal remains pending. Full imported initiative 20 remains unresolved because its Saga-style/technology-infused umbrella is not this one historical model. School/operator identities, structured dose and grade/cohort mapping, remaining outcomes and cost-accounting review remain unfinished.
