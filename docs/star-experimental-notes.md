# STAR experimental extraction

Verified 2026-09-11 against the published [Krueger 1999 paper](https://hceconomics.uchicago.edu/sites/default/files/events/Krueger_1999_QJE_v114_n2.pdf), Table V, pages 512–513. The runtime source file is `data/education/star-experimental-extraction.json`.

Eight rows use column 7 consistently: initial-assignment effects for small classes and regular classes with full-time aides, each relative to regular classes, at four grade endpoints. Robust standard errors account for classmates' correlated residuals. The sample sizes refer to all analysis arms within each grade, not to the number receiving that intervention. Column 8 adds teacher covariates and must not be silently mixed into column 7.

The important unit distinction is **percentile points**. These numeric effects must not use percentage formatting or be directly ranked against Saga's standard-deviation outcomes. A standard-deviation conversion requires its own verified denominator and provenance. Nor are they the economic model's assumed 0.20 SD inputs.

The eight rows are related estimates from one experiment, not independent replications. The teacher-aide comparison is substantively different from class-size reduction; the model's class-size costs cannot be assigned to it. Keep the experimental publication linked to its working-paper version and the later economic publication without collapsing those different analyses.

All eight extracted estimates are now published and verified in [the STAR experimental dataset](https://www.geobrowser.io/space/dac259bad48a11adf97fe36857d85206/9220554acfd249a18920b301dcbb6cf0). The identity and modeling notes below preserve the review history; the publication evidence at the end supersedes their earlier pending status.

Identity follow-up: the all-space text-value probe for working-paper DOI `10.3386/w6051` completed with zero matches; saved as `star-experimental-working-paper-discovery.json`. Exact-name ontology discovery found four Study design properties and one unrelated Topic, so no new Study design property should be created without reviewing existing data types, examples and source-space provenance. Candidate evidence is in `discovery/study-design.json`. Exact Grade level, School grade, Intervention arms and Treatment arms probes found no candidates; alias/semantic discovery is still required before allocating properties.

## Study-category reuse review — 2026-09-11

Live evidence in [star-study-category-review.json](../data/education/star-study-category-review.json) confirms that Health's **Study categories** property (`76cac7bf35d44162a2961b12e99dc1a1`) is a relation targeting **Study category** (`07d23153ead146268af89189abe0b6f3`). Its Health-authored definition covers research approaches generally, and sampled uses link actual publications to design categories. Reuse this property for structured research-design filtering rather than creating another Study design property. The usage sample is bounded; it does not claim to enumerate every use.

The complete nine-category inventory is in [star-study-category-candidates.json](../data/education/star-study-category-candidates.json). It includes an existing RCT category (`82853715d57840fb93f79036e8acdad4`), distinct from the glossary RCT entity (`ad9f48f68b804ddd9f9e8ffa5869ef02`). The latter currently has Evidence and Topic types, not Study category. Do not assume two identically named entities are interchangeable targets or create a third RCT entity.

[star-rct-category-review.json](../data/education/star-rct-category-review.json) confirms the category's type and all three existing Study categories links, including a publication explicitly named as a randomized controlled trial. **Graph-quality issue:** the category has no canonical description; its semantic definition remains unverified. Existing use supports a candidate mapping but does not supply the missing definition. Keep the intended RCT mapping flagged for description/identity review; do not overwrite Health's facts or silently merge the two RCT IDs. The broader Controlled experiment category has a matching definition, but substituting that alone would lose the requested randomized-design detail.

The four Study design properties reviewed in `study-design-candidate-review.json` comprise three text properties and one relation with no definition and mixed design/publication targets. They offer no reason to allocate another synonym. Grade and intervention-arm modeling remains a separate prerequisite: earlier empty exact-name probes, and substring hits that are Claims or unrelated Topics, do not establish absence of all semantic alternatives.

Additional all-space substring probes completed for School year (42 candidates), Education stage (0), Study arms (0), Experimental group (2), Treatment group (8), and Comparison group (5). None returned a Property or Type candidate. The comparison-group data block is a curated clinical table, not a reusable arm property. These results narrow the remaining semantic search; they do not justify repurposing clinical treatments or conflating calendar school years with grade levels. Evidence is saved in `data/education/discovery/contains-*.json` under the corresponding query names.

## Publication model resolved — 2026-09-11

`star-experimental-model.json` and `education-build-star-experimental.ts` implement one Study, three study-specific assignment arms, four US grade concepts, and eight outcome estimates linked to their intervention and comparator. Completed exact checks for Study arm, Trial arm, Cohort, Intervention arms, Comparison arms, Grade levels, Kindergarten, First/Second/Third grade and Grade 1/2/3 found no matching entities. Substring grade searches returned news, claims, images and blocks rather than grade concepts. Canonical property synonym searches found no applicable grade or arm relations; medical Treatments and Condition properties have incompatible definitions. Create Study arm as a specific Type and Grade levels, Intervention arms and Comparison arms as relation properties with declared target types.

Health's Population type is defined for demographic groups stratifying reference ranges, not randomized assignment arms (`star-population-review.json`). Reusing it for arms would be a semantic mismatch. The existing generic Study type `3ef269bc5f114691abc02dcbf398fd63` has a broad scientific-investigation definition in its source space and no sampled instances (`star-study-type-review.json`); reuse that instead of Health's narrower same-name design classification. These new study facts do not modify either type definition.

The existing RCT category has three verified classification uses, one explicitly for a randomized controlled trial. The pilot reuses that category and supplies a standard random-assignment definition **only in Education datasets**; the missing Health definition remains a graph-quality issue, but it no longer prevents a locally explicit mapping. The glossary Evidence/Topic entity remains separate; no merge or third RCT is created. This resolves the candidate-only status above for this publication model.

The builder reads extraction and model files at runtime, preserves IDs and fractional positions, checks declared scalar/relation data types and encoded effect/SE/N values, and refuses to overwrite a journaled payload. The SDK meta-type constant is `SystemIds.SCHEMA_TYPE`, not `SystemIds.TYPE`; the initial local typecheck caught this and it was corrected before publication. Existing Claim type schemas are not changed globally; the published properties are explicit per-estimate facts, displayed through the dataset table. Full program/implementation/population/instrument links remain unfinished under the dashboard contract.

A broad author-name probe for Alan hit the 200-candidate safety bound and is explicitly incomplete. It is not evidence of author absence; this batch creates no Person. Author attribution is retained in the publication block while a narrower identity check can resolve an Authors relation later.

## Applied and verified — 2026-09-11

- Dataset `9220554acfd249a18920b301dcbb6cf0`; study `d68c6d1d10af47418fc80ed09d4e097f`; publication `efdefdaf3fbd4d02bd44228882c09359`.
- Pilot proposal `490f4f13922b415395650b09e175ba46`: 78 operations, executed; proposal transaction `0x4da0484227500ef17070554971e42f354971c5931f4e7a656bfb71bcbfcca8f1`, bounty transaction `0x7b24f65ad2cc757b214a2c2249a5b86a01d4c421d0f7a9fca846d4628d0e65b4`, execution vote `0x6583e675d27191b2f4cf648bfb4375c6b4b754ec7881049d244861efe175eced`.
- Remaining-seven proposal `2ce99c63d1ac4276a0e90d8a3b537102`: 70 operations, executed; proposal transaction `0x340cdb9ebad39d7425465b332495165048047d9c510bde4e219a4c127cb0297f`, bounty transaction `0xb48624ff9cb92b0bdc394be991a7147effd1c3afa1dc5acf9e9d4f72fa3d3b9b`, execution vote `0x333290cba9165279219c84464567946239551d1d0ea7c3c0d4180c245875b179`.
- Pilot API verification passes 105 checks; expansion passes 128, covering values, exact relation and relation-entity IDs, positions, executed proposals and indexed bounty links. Both `star-experimental-*-index-verification.json` reports and publication journals are retained.
- Live browser review of all eight rows confirms effects, standard errors, sample sizes, grade/arm links, comparator, unit, source locators and source-table order. Evidence: `star-experimental-full-visual.json`. Default numeric display drops trailing zeroes but retains values.
- `star-comparison-query.graphql` with `education-verify-star-filters.ts` passes eight live filter cases: all estimates, four grades, two arms and grade 1 × small class. Every response is destination-scoped and complete; effect/SE/N/unit and comparator links are checked against extraction. Results: `star-comparison-query-verification.json`.

The first expansion verification ran before indexing caught up and returned a missing entity. Re-reading after indexing succeeded; no repeat publication was necessary. Likewise, a page opened before indexing retained the one-row pilot until fresh navigation. Transaction success, API indexing and browser state are separate checks. These observations supersede any blanket assumption that every missing entity is returned as a non-null stub.

This completes the eight-row extraction's publication, not the full bounty/dashboard objective. Remaining work includes broader program/implementation/population/instrument links, the three economic return estimates, other landmark studies, the full imported inventory and Curator-side bounty presentation.
# Claim readability correction — 2026-09-12 UTC

The user's screenshot exposed an unresolved publishing defect: all eight experimental Claims still had row-label titles (`STAR kindergarten: small class assignment effect`) and generic descriptions that omitted the estimate. Earlier numeric and table-rendering checks did not prove a readable Claim. The existing heuristic audit could flag these names, but the prior repair scope did not finish them; this was not evidence that the original wording was acceptable.

Re-read Krueger (1999), Table V column 7 (printed pp.512–513), both rendered table pages and discussion p.514. Kindergarten small-class initial assignment is +5.36 percentile points, class-correlated robust SE 1.21, relative to regular classes without a full-time aide. The outcome is average Stanford subject percentile rank, not a percentage increase or SD effect. The paper describes small-class gains across grades and a statistically significant aide gap in only one grade (grade 1); it also discusses part-time aide access as a limitation. Corrected titles state the actual grade-specific finding. Aide titles for K/2/3 state that no clear improvement was established; descriptions retain their positive point estimates and uncertainty. Grade 1's aide result must not become a claim of consistent gains across grades.

`src/education-star-claim-copy.ts` now supplies source-reviewed copy to both the original builder and `education-build-star-claim-copy.ts`. The repair preserves existing IDs, numeric fields, source/study/grade/arm links and table structure; it changes names, concise descriptions and Is factual=true. The live field is `Checkbox`, encoded as SDK `boolean`, so these checkable findings use Verify/Dispute. The repair checks source PDF SHA-256 `b970ac64cb6f3b04a941c23cdbfc1c92ce75f8d753f23995e761772eec18c93e`, current text and magnitude/SE against the source. Build passes 38 checks and TypeScript checks. Submitted historical payloads remain immutable.

Fresh full batch-derived audit before this repair: 334 Claims, 124 heuristic review flags (`claim-readability-audit.json`). This includes all eight STAR experimental Claims plus unfinished Perry, Reading First and modeled-return reviews, with possible false positives. Neither unflagged copy nor a short description alone proves a source-backed proposition. Keep the broader review open and distinguish completed repairs from outstanding work.
# Claim repair applied — 2026-09-12 UTC

Post-repair audit: 334 Claims, 119 heuristic flags. Three corrected STAR aide titles remain flagged because the verb whitelist does not recognize `did not clearly improve`; these are source-reviewed false positives, not unfinished repairs. Browser tab 73 confirms the kindergarten aide Claim explicitly states no clear improvement, retains +0.53 percentile points with SE 1.09, explains the grade-1-only significant result and links the Article. Do not suppress uncertainty to satisfy a title heuristic.

All eight experimental Claims are corrected in place by executed proposal `7d564fc12ff042f2bd59217c85482c9e`. Main transaction `0xa1b7433042a550fb9ea66b17ac3ef65347622577182e8aaed7f0a636ea7c33c4`; indexed bounty-link transaction `0x619193531cac68dfb46c550e0895d555627e25fbebed0c916f68d962edf3fea8`; execution vote `0xbbae7be755045c21e6a4aee18470c33974bb8cbb3c1c9e7b4cf68a6376c7b352`. All 26 operation checks and eight existing STAR comparison-filter cases pass. The filter verifier reconciles unchanged numeric values, SEs, N, units, source and grade/arm links. TypeScript checking passes.

The dedicated kindergarten small-class Claim `7eeddeb79af54141b58e4cffe32d93a5` visibly shows the 5.36-percentile-point finding, SE 1.21, comparator, initial-assignment interpretation, correct source and Verify/Dispute (browser tab 72). Geo may collapse the description behind More; the essential finding is now in the title. Original experimental batches' name/description expectations are historical and superseded by `star-experimental-claim-copy-review.json`; do not rebuild submitted batches to erase that history. This repair completes the eight experimental records, not the global review or STAR's separate modeled economic Claims.
