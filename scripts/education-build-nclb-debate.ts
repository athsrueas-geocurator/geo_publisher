import{existsSync,readFileSync,writeFileSync}from'node:fs';import{createHash,randomUUID}from'node:crypto';import{Ops,Position,SystemIds,type Op}from'@geoprotocol/geo-sdk';import{geoGraphqlRequest as gql}from'../src/geo-api-client';import{EDUCATION_PUBLICATION as target}from'../src/education-bounty';
const root='data/education',prefix='nclb-debate';if(existsSync(`${root}/${prefix}-publication.json`))throw Error('Preserve submitted payload.');const rp=`${root}/${prefix}-registry.json`,r:Record<string,string>=existsSync(rp)?JSON.parse(readFileSync(rp,'utf8')):{},id=(k:string)=>r[k]??(r[k]=randomUUID().replaceAll('-',''));const p={claim:'96f859efa1ca4b229372c86ad58b694b',factual:'da4a6c1f9d4446f9832ff3b49a4400ef',source:'49c5d5e1679a4dbdbfd33f618f227c94',related:'dfa6aebe1ca94bf29faccc4cc7afb24c',support:'1dc6a843458848198e7a6e672268f811',oppose:'4e6ec5d14292498a84e5f607ca1a08ce'},article='d83f04c4e8594faeb657a226c26c134c',initiative='ed3fbb4f693c4785955b627ea98c03fb',support=['0bf6eec672674e449f3473ce134e77cd','8a6350c64b7b49ceb5860ef6baf77e6c'],oppose=['96e166271b344f27b932e83442381be3'],name='Federal test-based accountability should be judged as a math-specific achievement policy rather than a general reading reform.',description='Dee and Jacob’s NCLB study reports fourth- and eighth-grade math improvement, including a 0.22 fourth-grade effect-size estimate by 2007. The same source found no evidence of fourth- or eighth-grade reading improvement, so the evidence does not support a general achievement claim.';
const checks:string[]=[];for(const[property,type]of[[SystemIds.TYPES_PROPERTY,'Relation'],[p.factual,'Checkbox'],[p.source,'Relation'],[p.related,'Relation'],[p.support,'Relation'],[p.oppose,'Relation']]as const){const d:any=await gql('query($id:UUID!){property(id:$id){dataTypeName}}',{variables:{id:property}});if(d.property?.dataTypeName!==type)throw Error(`Schema ${property}`);checks.push(`Schema ${property}`)}const parent=id('parent'),found:any=await gql('query($id:UUID!,$name:String!){entity(id:$id){spaceIds}entitiesConnection(first:10,filter:{name:{isInsensitive:$name}}){nodes{id}pageInfo{hasNextPage}}}',{variables:{id:parent,name}});if(found.entity?.spaceIds?.length||found.entitiesConnection.pageInfo.hasNextPage||found.entitiesConnection.nodes.length)throw Error('Identity collision');for(const c of[...support,...oppose]){const d:any=await gql('query($id:UUID!,$space:UUID!){entity(id:$id){types{id}values(first:20,filter:{spaceId:{is:$space}}){nodes{propertyId boolean}}}}',{variables:{id:c,space:target.spaceId}});if(!d.entity?.types.some((t:any)=>t.id===p.claim)||!d.entity.values.nodes.some((v:any)=>v.propertyId===p.factual&&v.boolean===true))throw Error(`Evidence ${c}`)}const ops:Op[]=[];ops.push(...Ops.entities.update({id:parent,name,description,values:[{property:p.factual,type:'boolean',value:false}]}).ops);const edge=(k:string,f:string,t:string,to:string)=>{r[`p/${k}`]??=Position.generate();ops.push(...Ops.relations.create({id:id(`e/${k}`),entityId:id(`x/${k}`),fromEntity:f,type:t,toEntity:to,position:r[`p/${k}`]}).ops)};edge('type',parent,SystemIds.TYPES_PROPERTY,p.claim);edge('source',parent,p.source,article);edge('initiative',parent,p.related,initiative);for(const c of[...support,...oppose]){edge(`related/${c}`,parent,p.related,c);edge(`reverse/${c}`,c,p.related,parent)}for(const c of support)edge(`support/${c}`,parent,p.support,c);for(const c of oppose)edge(`oppose/${c}`,parent,p.oppose,c);const bytes=JSON.stringify(ops,(_k,v)=>v instanceof Uint8Array?{$bytes:Buffer.from(v).toString('hex')}:typeof v==='bigint'?{$bigint:v.toString()}:v,2)+'\n',sha256=createHash('sha256').update(bytes).digest('hex'),batch={name:'Add No Child Left Behind math-versus-reading debate claim',spaceId:target.spaceId,bounty:target.bountyId,opsPath:`${root}/${prefix}-ops.json`,sha256,journalPath:`${root}/${prefix}-publication.json`,validationPath:`${root}/${prefix}-validation.json`,entities:{parent,support,oppose},operationCount:ops.length};writeFileSync(rp,JSON.stringify(r,null,2)+'\n');writeFileSync(batch.opsPath,bytes);writeFileSync(`${root}/${prefix}-batch.json`,JSON.stringify(batch,null,2)+'\n');writeFileSync(batch.validationPath,JSON.stringify({ready:true,checkedAt:new Date().toISOString(),opsHash:sha256,checks},null,2)+'\n');console.log(JSON.stringify(batch,null,2));
