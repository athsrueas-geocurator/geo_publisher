import{existsSync,readFileSync,writeFileSync}from'node:fs';import{createHash}from'node:crypto';import{Ops,type Op}from'@geoprotocol/geo-sdk';import{geoGraphqlRequest as gql}from'../src/geo-api-client';import{EDUCATION_PUBLICATION as target}from'../src/education-bounty';
const root='data/education',prefix='project-quest-debate-copy';if(existsSync(`${root}/${prefix}-publication.json`))throw Error('Preserve submitted payload.');const parent='2ae4b31eee4e4434b54c85c1310e4806',description='Project QUEST’s randomized study reports lower annual earnings in years one and two and positive differences in years three through nine. It supports a nine-year horizon for this program, not a universal payoff or benefit-cost ratio.';const d:any=await gql('query($id:UUID!,$space:UUID!){entity(id:$id){name description types{id}values(first:30,filter:{spaceId:{is:$space}}){nodes{propertyId boolean}pageInfo{hasNextPage}}}}',{variables:{id:parent,space:target.spaceId}});if(!d.entity?.types.some((t:any)=>t.id==='96f859efa1ca4b229372c86ad58b694b'))throw Error('Parent is not a Claim.');if(d.entity.values.pageInfo.hasNextPage||!d.entity.values.nodes.some((v:any)=>v.propertyId==='da4a6c1f9d4446f9832ff3b49a4400ef'&&v.boolean===false))throw Error('Parent is not a nonfactual Claim.');const ops:Op[]=Ops.entities.update({id:parent,description}).ops,bytes=JSON.stringify(ops,(_k,v)=>v instanceof Uint8Array?{$bytes:Buffer.from(v).toString('hex')}:typeof v==='bigint'?{$bigint:v.toString()}:v,2)+'\n',sha256=createHash('sha256').update(bytes).digest('hex'),batch={name:'Shorten Project QUEST debate description',spaceId:target.spaceId,bounty:target.bountyId,opsPath:`${root}/${prefix}-ops.json`,sha256,journalPath:`${root}/${prefix}-publication.json`,validationPath:`${root}/${prefix}-validation.json`,entities:{parent},operationCount:ops.length};writeFileSync(batch.opsPath,bytes);writeFileSync(`${root}/${prefix}-batch.json`,JSON.stringify(batch,null,2)+'\n');writeFileSync(batch.validationPath,JSON.stringify({ready:true,checkedAt:new Date().toISOString(),opsHash:sha256,checks:['Existing parent is a nonfactual Claim','Copy has two sentences','Copy states the earnings pattern and its scope'],rationale:'Copy-only repair: retain the linked evidence and make the reader-facing description fit the two-sentence editorial guidance.'},null,2)+'\n');console.log(JSON.stringify(batch,null,2));
