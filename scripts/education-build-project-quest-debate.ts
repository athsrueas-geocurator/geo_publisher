import{existsSync,readFileSync,writeFileSync}from'node:fs';import{createHash,randomUUID}from'node:crypto';import{Ops,Position,SystemIds,type Op}from'@geoprotocol/geo-sdk';import{geoGraphqlRequest as gql}from'../src/geo-api-client';import{EDUCATION_PUBLICATION as target}from'../src/education-bounty';
const root='data/education',prefix='project-quest-debate';if(existsSync(`${root}/${prefix}-publication.json`))throw Error('Preserve submitted payload.');const rp=`${root}/${prefix}-registry.json`,r:Record<string,string>=existsSync(rp)?JSON.parse(readFileSync(rp,'utf8')):{},id=(k:string)=>r[k]??(r[k]=randomUUID().replaceAll('-',''));
const p={claim:'96f859efa1ca4b229372c86ad58b694b',factual:'da4a6c1f9d4446f9832ff3b49a4400ef',source:'49c5d5e1679a4dbdbfd33f618f227c94',related:'dfa6aebe1ca94bf29faccc4cc7afb24c',support:'1dc6a843458848198e7a6e672268f811'},article='83427dac98de4da496573df368a07561',initiative='f557a1af0d3846ca8beaec99f30b6e1c',name='Workforce programs should be evaluated long enough to capture earnings effects during training and after completion.',description='Project QUEST’s random-assignment study reports lower average annual earnings in its first two follow-up years and positive differences in years three through nine. The linked findings support judging this program over its reported nine-year horizon, while its single-site design and reported operating cost do not establish a universal payoff or a benefit-cost ratio.',support=['e04897d09dc14e8294f0667904d7bde8','5c7fa63ba54e454db3aab82101d31224','1b83f22bd4f74f3b8526f01fdfd66fdf','022c945ffb12436e9d98c5b808361691','ecfccdd3646a42ff932842c06b7ad1cf','80758bd7690546128f233124d4080018','bab671b1e148491d93384fbed1c5ed1d'],related=['a5d6539c8a3746a2a1a76dbe3e7687a0','ffc75a1e575344eca325405dd41ecf98'],checks:string[]=[],ops:Op[]=[],check=(x:unknown,s:string)=>{if(!x)throw Error(s);checks.push(s)};
for(const[q,t]of[[SystemIds.TYPES_PROPERTY,'Relation'],[p.factual,'Checkbox'],[p.source,'Relation'],[p.related,'Relation'],[p.support,'Relation']]as const){const d:any=await gql('query($id:UUID!){property(id:$id){dataTypeName}}',{variables:{id:q}});check(d.property?.dataTypeName===t,`Live ${t}: ${q}`)}
const parent=id('parent'),d:any=await gql('query($id:UUID!,$name:String!){entity(id:$id){spaceIds}entitiesConnection(first:10,filter:{name:{isInsensitive:$name}}){nodes{id}pageInfo{hasNextPage}}}',{variables:{id:parent,name}});check(!d.entity?.spaceIds?.length,'Allocated parent unused');check(!d.entitiesConnection.pageInfo.hasNextPage&&!d.entitiesConnection.nodes.length,'All-space parent discovery has no candidate');
for(const claim of[...support,...related]){const x:any=await gql('query($id:UUID!,$space:UUID!){entity(id:$id){types{id}values(first:30,filter:{spaceId:{is:$space}}){nodes{propertyId boolean}pageInfo{hasNextPage}}}}',{variables:{id:claim,space:target.spaceId}});check(x.entity?.types.some((t:any)=>t.id===p.claim)&&!x.entity.values.pageInfo.hasNextPage&&x.entity.values.nodes.some((v:any)=>v.propertyId===p.factual&&v.boolean===true),`Factual evidence: ${claim}`)}
const edge=(k:string,from:string,type:string,to:string)=>ops.push(...Ops.relations.create({id:id(`edge/${k}`),entityId:id(`relation/${k}`),fromEntity:from,type,toEntity:to,position:r[`position/${k}`]??(r[`position/${k}`]=Position.generate())}).ops);ops.push(...Ops.entities.update({id:parent,name,description,values:[{property:p.factual,type:'boolean',value:false}]}).ops);edge('type',parent,SystemIds.TYPES_PROPERTY,p.claim);edge('source',parent,p.source,article);edge('initiative',parent,p.related,initiative);for(const claim of[...support,...related]){edge(`related/${claim}`,parent,p.related,claim);edge(`related-reverse/${claim}`,claim,p.related,parent)}for(const claim of support)edge(`support/${claim}`,parent,p.support,claim);
const bytes=JSON.stringify(ops,(_k,v)=>v instanceof Uint8Array?{$bytes:Buffer.from(v).toString('hex')}:typeof v==='bigint'?{$bigint:v.toString()}:v,2)+'\n',sha256=createHash('sha256').update(bytes).digest('hex'),batch={name:'Add Project QUEST long-horizon evaluation debate claim',spaceId:target.spaceId,bounty:target.bountyId,opsPath:`${root}/${prefix}-ops.json`,sha256,journalPath:`${root}/${prefix}-publication.json`,validationPath:`${root}/${prefix}-validation.json`,entities:{parent,article,initiative,support,related},operationCount:ops.length};writeFileSync(rp,JSON.stringify(r,null,2)+'\n');writeFileSync(batch.opsPath,bytes);writeFileSync(`${root}/${prefix}-batch.json`,JSON.stringify(batch,null,2)+'\n');writeFileSync(batch.validationPath,JSON.stringify({ready:true,checkedAt:new Date().toISOString(),opsHash:sha256,checks,rationale:'Nonfactual policy proposition linked to the full source-specific earnings trajectory. Early losses are Related context; later positive observations are Support. The text does not claim a benefit-cost ratio or universal effect.'},null,2)+'\n');console.log(JSON.stringify(batch,null,2));
