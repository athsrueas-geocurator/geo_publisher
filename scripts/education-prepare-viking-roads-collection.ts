import {readFileSync,writeFileSync} from 'node:fs';
const root='data/education';const batch:any=JSON.parse(readFileSync(`${root}/viking-roads-2025-batch.json`,'utf8'));const ops:any[]=JSON.parse(readFileSync(`${root}/viking-roads-2025-ops.json`,'utf8'));const ids=Object.values(batch.entities.claims as Record<string,string>) as string[];
const prop={name:'a126ca530c8e48d5b88882c734c38935',description:'9b1f76ff9711404c861e59dc3fa7d037',n:'210e9b352b454d6eb7a32906d24d3c8d',measure:'8405509cc7354655a348591349a5f025',locator:'84dacbddca6a44079edb5e11a4c66b40'};const b=(x:any)=>x?.['$bytes'];const text=(x:any)=>x?.value;const integer=(x:any)=>x?.value?.['$bigint'];const updates=new Map<string,any>();for(const op of ops)if(op.type==='updateEntity')updates.set(b(op.id),op);
const facts=ids.map(id=>{const op=updates.get(id);if(!op)throw Error(`Missing Claim update ${id}`);const values=new Map<string,any>();for(const item of op.set??[])values.set(b(item.property),item.value);return{id,sourceId:batch.entities.article,locator:text(values.get(prop.locator)),classification:'observed',context:'Viking ROADS three-year randomized evaluation at SUNY Westchester; outcome horizons and the COVID-era setting remain explicit.',name:text(values.get(prop.name)),description:text(values.get(prop.description)),values:[{propertyId:prop.n,field:'integer',value:integer(values.get(prop.n))},{propertyId:prop.measure,field:'text',value:text(values.get(prop.measure))}]};});
const plan={version:1,key:'viking-roads-collection',spaceId:batch.spaceId,bounty:batch.bounty,catalogId:'2279edef1bbe479c872caeb72ee90022',name:'Viking ROADS three-year findings',description:'Three-year randomized findings from the Viking ROADS student-support evaluation.',notes:'Rows reuse existing Claims and retain explicit semester horizons; this report has no complete cost analysis.',sourceIds:[batch.entities.article],relatedIds:[batch.entities.initiative],groups:[{key:'findings',name:'Viking ROADS findings',columns:[{id:prop.n,dataType:'Integer'},{id:prop.measure,dataType:'Text'}],members:ids}]};writeFileSync(`${root}/viking-roads-collection-plan.json`,JSON.stringify(plan,null,2)+'\n');writeFileSync(`${root}/viking-roads-collection-facts.json`,JSON.stringify(facts,null,2)+'\n');console.log(JSON.stringify({claims:facts.length,columns:2}));
