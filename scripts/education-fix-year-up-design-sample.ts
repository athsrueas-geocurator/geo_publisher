import{existsSync,readFileSync,writeFileSync}from'node:fs';import{createHash}from'node:crypto';import{Ops,type Op}from'@geoprotocol/geo-sdk';import{geoGraphqlRequest as gql}from'../src/geo-api-client';import{EDUCATION_PUBLICATION as target}from'../src/education-bounty';const root='data/education',prefix='year-up-design-sample-fix-v2',sample='210e9b352b454d6eb7a32906d24d3c8d';if(existsSync(`${root}/${prefix}-publication.json`))throw Error('Preserve submitted payload.');const r:Record<string,string>=JSON.parse(readFileSync(`${root}/year-up-outcomes-registry.json`,'utf8')),id=r['claim/design'];const d:any=await gql('query($id:UUID!){property(id:$id){dataTypeName}}',{variables:{id:sample}});if(d.property?.dataTypeName!=='Integer')throw Error('Sample schema mismatch');const ops:Op[]=Ops.entities.update({id,values:[{property:sample,type:'integer',value:2544n}]}).ops,bytes=JSON.stringify(ops,(_k,v)=>v instanceof Uint8Array?{$bytes:Buffer.from(v).toString('hex')}:typeof v==='bigint'?{$bigint:v.toString()}:v,2)+'\n',sha256=createHash('sha256').update(bytes).digest('hex'),batch={name:'Correct Year Up randomized design sample to 2,544 applicants',spaceId:target.spaceId,bounty:target.bountyId,opsPath:`${root}/${prefix}-ops.json`,sha256,journalPath:`${root}/${prefix}-publication.json`,validationPath:`${root}/${prefix}-validation.json`,entities:{claim:id},operationCount:ops.length};writeFileSync(batch.opsPath,bytes);writeFileSync(`${root}/${prefix}-batch.json`,JSON.stringify(batch,null,2)+'\n');writeFileSync(batch.validationPath,JSON.stringify({ready:true,checkedAt:new Date().toISOString(),opsHash:sha256,source:'Fein and Dastrup (2022), report pp. 3–4, 11: 2,544 eligible applicants randomly assigned. The 2,495 NDNH analytic sample remains appropriate only for earnings outcomes.'},null,2)+'\n');console.log(JSON.stringify(batch,null,2));
